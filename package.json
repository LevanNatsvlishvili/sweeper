{
  "name": "sweet-swap",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "size": "npm run build && node scripts/size.mjs",
    "portfolio": "npm run build && node scripts/build-portfolio.mjs",
    "capture": "node scripts/capture-demos.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "pixi.js": "^8.17.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.7.3",
    "vite": "^7.0.0",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^4.1.11"
  }
}
